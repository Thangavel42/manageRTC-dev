# ==========================================
# manageRTC Backend - Environment Variables
# ==========================================
# Copy this file to .env and fill in the values

# ------------------------------------------
# Application Configuration
# ------------------------------------------
NODE_ENV=production
PORT=5000

# ------------------------------------------
# Database Configuration
# ------------------------------------------
# MongoDB connection strings (REQUIRED — no hardcoded fallback)
# Atlas: mongodb+srv://username:password@cluster.mongodb.net/?appName=myApp
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/?appName=myApp
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?appName=myApp
MONGODB_DATABASE=AmasQIS

# ------------------------------------------
# Authentication - Clerk
# ------------------------------------------
# Get these from your Clerk Dashboard: https://dashboard.clerk.com
CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
CLERK_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Clerk JWT verification (REQUIRED for Socket.IO authentication)
CLERK_JWT_KEY=your-jwt-key-from-clerk-dashboard

# Deprecated: Old JWT key format
# JWT_KEY=your-secret-key-for-jwt-verification

# ------------------------------------------
# Frontend Configuration
# ------------------------------------------
# Primary frontend URL for CORS (REQUIRED)
FRONTEND_URL=http://localhost:3000

# Additional allowed CORS/Socket.IO origins (comma-separated, no spaces)
# Example: https://apiacc.manage-rtc.com,https://other.example.com
EXTRA_ALLOWED_ORIGINS=

# ------------------------------------------
# Email Configuration (Optional)
# ------------------------------------------
# SMTP settings for sending emails (notifications, etc.)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@managertc.com

# ------------------------------------------
# File Upload Configuration
# ------------------------------------------
# Maximum file upload size (in bytes)
# Default: 50MB
MAX_FILE_SIZE=52428800

# Allowed file extensions (comma-separated)
ALLOWED_FILE_TYPES=.jpg,.jpeg,.png,.pdf,.doc,.docx,.xls,.xlsx

# ------------------------------------------
# Redis Configuration (Optional)
# ------------------------------------------
# Redis connection for caching and session management
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=redis123

# ------------------------------------------
# Socket.IO Configuration
# ------------------------------------------
# Socket.IO CORS settings
SOCKET_CORS_ORIGIN=http://localhost:3000

# Socket.IO path (default: /socket.io)
SOCKET_PATH=/socket.io

# ------------------------------------------
# Rate Limiting
# ------------------------------------------
# Enable/disable rate limiting
RATE_LIMIT_ENABLED=true

# Rate limit window (milliseconds)
RATE_LIMIT_WINDOW=900000

# Max requests per window
RATE_LIMIT_MAX_REQUESTS=100

# ------------------------------------------
# Logging Configuration
# ------------------------------------------
# Log level: error, warn, info, debug
LOG_LEVEL=info

# Enable request logging
LOG_REQUESTS=true

# ------------------------------------------
# Security Configuration
# ------------------------------------------
# Session secret for express-session
SESSION_SECRET=your-random-session-secret-here

# JWT expiration time (e.g., 7d, 24h, 60m)
JWT_EXPIRES_IN=7d

# ------------------------------------------
# External Services (Optional)
# ------------------------------------------
# Sentry DSN for error tracking
SENTRY_DSN=

# Google Analytics tracking ID
GA_TRACKING_ID=

# ------------------------------------------
# Development Settings
# ------------------------------------------
# Enable development mode (bypasses some security checks)
# WARNING: Never set to true in production!
# This affects rate limiting and companyId auto-assignment in Socket.IO
DEV_MODE=false

# Enable debug logging
DEBUG=false

# Enable debug logging
DEBUG=false

# Enable Swagger API documentation
SWAGGER_ENABLED=true
